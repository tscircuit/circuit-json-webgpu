These 13 paired snapshots use the Linux SVG font fallback for Arial/sans-serif.
The GPU halves matched the shared baselines (zero antialias-filtered pixel differences).
All other feature snapshots share the same baseline across platforms.
Only the SVG reference rasterization differs; no GPU tolerances are relaxed.
