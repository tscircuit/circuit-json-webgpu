The text layout helpers in `lib/text/getAlphabetLayout.ts`,
`getAlphabetOutlineGroups.ts`, `getTextStartPosition.ts`, and
`getPolygonBounds.ts` are copied from tscircuit/circuit-to-canvas at
fa8405c67634e287b79a1506eedde077ef6accb0. The original MIT license is retained
at `tests/upstream/circuit-to-canvas/LICENSE`.

`tests/upstream/circuit-to-canvas` preserves that repository's complete `tests/`
and `lib/` trees. Its manifest records every original file's SHA-256 hash. The
reference implementation is test-only; it is never imported by the GPU renderer.
